// Join Game
/* document.onload = function (socket) {
    socket.emit('join game', gameID, sessID);

    // Join Red Commands Process
    joinRedCommandsBtn.addEventListener("click", function (e) {
        e.preventDefault();
        socket.emit('joinRedCommandersReq', gameID, sessID);

    });

    socket.on('joinRedCommadRes', function (msg) {
        console.log(object);
    });
}

socket.on('join game res', function (spectators) {
    console.log(spectators);
    console.log('asd');
}); */
